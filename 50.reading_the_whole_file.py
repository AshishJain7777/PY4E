fhand = open('calculator_py.txt')
inp = fhand.read() #.read() is used to read the given file. It store the readable file in a variable.
print(len(inp)) #len is used to determine the length of the whole document.
print(inp[:20]) # it start from the beginning to the 20th valued charachter.

