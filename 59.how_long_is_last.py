great = "hello ashish"
print(len(great))  # at this point we can see that len sum up the total length of the given variable. In this len calculate that how much charachter there are.
x = [1,2,"ashish",99.4] # we stored the list in this variable called "x"
print(len(x)) # at here the len function calculate the whole list, if we emphasis on the list so it meant the words that holds the poistions respectively. At this point len calculate how much item in there in the list.